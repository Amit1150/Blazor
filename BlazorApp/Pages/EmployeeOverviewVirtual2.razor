@page "/employeeoverviewvirtual2"

@if (Employees == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <div class="virtualized">
        <table class="table">
            <thead>
                <tr>
                    <th></th>
                    <th>Employee ID</th>
                    <th>First name</th>
                    <th>Last name</th>
                </tr>
            </thead>
            <tbody>
                <Virtualize ItemsProvider="LoadEmployees" ItemSize="itemHeight">
                    <ItemContent>
                        <tr @key="context.GetHashCode()">
                            <td><img src="@(context.ImageName)" class="rounded-circle" width="75" /> </td>
                            <td>@context.EmployeeId</td>
                            <td>@context.FirstName</td>
                            <td>@context.LastName</td>
                        </tr>
                    </ItemContent>
                    <Placeholder>
                        <tr>
                            <td>Loading...</td>
                            <td>Loading...</td>
                            <td>Loading...</td>
                        </tr>
                    </Placeholder>
                </Virtualize>
            </tbody>
        </table>
    </div>
}